---
category: Layout
---

# Divider

섹션 구분선(`.d-divider`). 1px 연한 경계선에 위아래 32px 여백이 붙는다.

```tsx
<section>…</section>
<Divider />
<section>…</section>
```

## 주의

여백이 이미 포함되어 있으므로 위아래에 `.d-mt-lg` 등을 추가로 주면 간격이 두 배가 된다.
