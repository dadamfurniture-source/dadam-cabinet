# 도면↔BOM 원장 (C1a) — 3D 부재와 BOM 행의 1:1 대조 보고

> 계획: `docs/01-plan/detail-bom-deepening.plan.md` §4.5 (BOM 은 부재 모델로 수렴한다) · §5 C1a
> 시험: `__tests__/scene-bom-ledger.test.js` · 허용 목록: `test-utils/ledger-allowlist.json`
> 유틸: `test-utils/scene-parts.js` (3D) · `test-utils/bom-parts.js` (BOM) · `test-utils/ledger-diff.js` (대조)
> 이 문서는 C1b(`js/planner/part-model.js`)가 **무엇을 먼저 옮길지** 정하는 입력이다.

## 0. 한 문단 요약

"도면에 나타난 모든 자재를 정확히" 는 3D 가 그린 부재 목록과 BOM 이 센 부재 목록이 **같은 목록**일 때만 참이다.
지금은 아니다. 골든 3벌 + ㄱ자 코너 배치를 실제 경로(전체 자동계산 → `renderAll3D` / `buildPlannerPayload` →
`_convertPlannerModules` → `MaterialExtractor.extract`)로 흘려 (플래너 모듈, 부재 종류) 묶음을 대조한다.
C1a 첫 보고(#643)에서는 362 묶음 중 **53 묶음만 맞고 309 건이 어긋났다.** 측판·표준 하부장 도어·멍장 목대·키큰장 하부 단 도어는 이미 같았다.
어긋남은 원인 15 묶음으로 나뉘었고, 그중 **키큰장 단이 하부장 규칙을 타는 것**(도어 H−30 · 선반 강제 · 걸레받이
폭에 3단이 다 더해짐)과 **상부장 없는 배치의 상몰딩**은 지금 발주가 틀리는 BOM 버그였다. 나머지는 3D 가 그리지 않거나
단순화한 것(밴드·뒷판 2.7T·사쿠리·라인 단위 부재)과 3D 쪽 오류(상부 도어 내림 잘림·멍장 도어 15 과다)다.

**P1 (이 문서 §4 1–3) + 분배기 결정(#648) 뒤**: 353 묶음 중 **57 일치 · 296 차이**. 분배기 표시 모듈이 장이 아니게 되어 #8 가전 영역 모듈 14건이 사라졌고, BOM 버그 두 묶음(#14 키큰장 단 규칙 3건, #1 의 상몰딩 1건)과
키큰장 목찬넬·상몰딩 3건이 사라졌고, 새로 낸 상판·좌대·상몰딩 행이 3D 의 **묶음 단위·모델 단순화**와 어긋나는 8건이 들어왔다
(상판 4: 3D 배치 공간마다 vs BOM 품목 한 장 · 좌대 3: 3D 통짜 블록 vs BOM 상자 · 키큰장 상몰딩 1: 3D 블록 vs BOM 면판).
건수는 309 → 296 이고 **발주가 틀리는 항목은 0** 이다 — 남은 것은 전부 단순화·3D 오류·규칙 확인이다.

**P2 (§4 4–6, `agent/planner-p2-fixes`) 뒤**: 353 묶음 중 **99 일치 · 254 차이**. 3D 오류 세 원인이 전부 사라졌다 —
상부 도어 내림 잘림 8건(도어 H 가 BOM 과 같은 장H+15), 멍장 정면 치수 3건(멍가림판·마감재 몸통 H, 도어 doorW−4), 처짐방지목 31건
(70 × (H−2T) · 폭 문턱 2장 · 단문 포함 · 목찬넬 −70 — 33 모듈 전부의 `body:brace` 가 맞는다). 296 → 254 (−42),
`buildPlannerPayload` 골든은 바이트 그대로(순수 3D 그리기 변경). 남은 254건은 **단순화 246 + 확인 필요 8(스펙 마감재)** 이다 —
3D 오류 판정은 0 이다.
차이는 허용 목록에 이유와 함께 **전부** 기록됐고, 시험은 새 차이도 낡은 항목도 막는다 — 항목은 코드를 고쳐야만 줄어든다.

**멍장 도어 결정 (2026-09-15, `agent/bom-blind-door-covers-batten`) 뒤**: **255 차이 (+1)**. P2 의 열린 질문 1(§3 #13 · §4 항목 5 "§3.5.1 과
BOM doorW−4 중 정본")이 사용자 결정으로 닫혔다 — **멍장 도어는 목대를 덮는다**, 재단 = doorW + 목대 15 − 갭 4 = **doorW + 11**
(corner.md §3.5.2). BOM 이 먼저 옮겨 cornerL 하부 멍장 도어 1건이 다시 어긋났다(3D 414 vs BOM 429) — 3D 가 옛 그림이라
플래너 후속 PR 이 맞추기로 하고 결정을 이유로 허용 목록에 남겼다.

**플래너 후속 (`agent/planner-blind-door-width`) 뒤**: **254 차이 (−1)**. `createModuleMesh` 도어 칸을 표준 도어와 같은
`칸 폭 − 4 = doorW + 11`(갭 좌우 2)로 되돌려 cornerL 멍장 도어가 3D 429 = BOM 429 로 맞는다. 멍 칸(`colW × 몸통 H`, 갭 없음)은
P2-5 그대로. 2D 정면도(`renderFrontView`)의 도어 rect 는 원래 `칸 폭 − 4` 였고 멍판 box 기준만 칸 시작으로 옮겨 3D 와 같이
갭 없이 덮는다 — 원장이 2D 를 보지 않으므로 `planner-blind-door-width.test.js` 가 2D·3D 를 따로 잠근다. "멍장 정면 치수" 묶음은
0 이고 새 항목은 없다. `buildPlannerPayload` 골든은 바이트 그대로.

**런 부재 멤버십 (2026-09-16, `agent/planner-run-part-membership`) 뒤**: **254 건 그대로 — 원장은 움직이지 않는다.**
걸레받이 폭이 NaN 이 되고 상판이 모듈마다 겹치던 버그(§4 아래 "런 부재 멤버십")를 고쳤지만, 이 원장의 픽스처는
`bootPlanner3D` 가 `modules:false` + 전체 자동계산으로 만든 모듈이라 **전부 `areaId` 를 갖는다**. 버그는 `areaId` 가
**없는** 옛 설계에서만 나타나므로 원장 픽스처에는 닿지 않는다 (§6 한계 참조). 그 경로는
`__tests__/planner-run-part-geometry.test.js` 가 따로 잠근다.

**처짐방지목 규칙 확정 (2026-09-19, `agent/brace-one-center`)**: **254 건 그대로 — 원장은 움직이지 않는다.** P2-6 (§4 항목 6)
이 자리를 `[확인 필요] 실제 조립 위치` 로 남겨 둔 것이 사용자 결정으로 닫혔다 — **폭 문턱은 장수가 아니라 유무다.**
상부장 W≥700 · 하부장·키큰장 단 W≥800 인 모듈에만 **1장, 모듈 중앙**이고 미만이면 부재가 아예 없다. 그때까지는 문턱
이상이면 2장·미만이면 1장이어서, 단문에도 한 장이 나갔고 양문에는 두 장이 나가 3D 가 그 둘을 폭의 1/3·2/3 —
도어 한 장씩의 가운데 뒤 — 에 세웠다. 문턱을 넘는 모듈은 도어 분배(350~600) 상 전부 양문이므로 이 규칙은 "양문이면
도어 두 장이 맞닿는 가운데 한 장" 과 같은 말이다. BOM(`extractors.js bomBraceQtyOf`)과 3D(`addBraces`)를 **같은 PR 에서**
옮겨 `body:brace` 는 계속 일치한다 — 허용 목록에 새 항목도, 지울 항목도 없다 (straight 픽스처: 800·900·932·1000 은 1장
중앙, 400·466 은 없음 — 3D·BOM 같다). 골든의 밴드(처짐방지) 행과 add-only 기준 다이제스트를 함께 다시 굳혔다.
2D 정면도는 양문 칸 가운데 한 장을 그대로 그린다 — 문턱을 넘는 모듈이 곧 양문이라 **규칙과 같은 그림**이다.

## 1. 방법

### 1.1 3D 쪽 — `test-utils/scene-parts.js`

- 플래너는 `if (three || !window.THREE) return;` 으로 three 가 없으면 3D 를 건너뛴다. `require('three')`(CJS 빌드)를
  `window.THREE` 에 올리고 WebGL 이 필요한 `WebGLRenderer` 와 addons 의 `OrbitControls` 만 스텁으로 바꾼다.
  기하·userData 를 만드는 코드는 renderer 를 쓰지 않으므로 **부재 목록은 브라우저와 같다** (그리기만 안 한다).
- `bootPlanner3D(layout)` = `bootPlanner` + `autoCalcAllAreas()` + `renderAll3D({fit:false})` (자동계산 안의 renderAll3D 는
  try/catch 로 삼키므로 직접 한 번 더 부른다).
- `three.moduleGroup` 을 순회해 Mesh 만 센다. `PLANNER_FINISH_PAINT_SKIP` (`edge` `reveal` `doorEdge` `carcass-line`
  `pick` `area` `module` `leg`) 는 표시용이거나 철물(다리발)이라 뺀다. `open` 칸의 와이어프레임 틀은
  `plannerFinishPartKeyOf` 가 null 을 주므로 빠진다. 나머지는 전부 — 먹장·멍판·목찬넬·좌대·상판 포함.
- 행 = `{ moduleId, family, a ≥ b, t, qty }`. `a,b` 는 두께 `t`(가장 짧은 변)를 뺀 두 변(큰 순) — BOM 이 w/h 를
  어느 쪽으로 적든 **순서 없는 쌍**으로 비교한다. `family` 는 partKey 에서 순번(`#…`)을 떼고 BOM 이름으로 접은 것:
  `body:left/right → body:side`, `body:back → back`, `channel:channelFace/Base → channel:front/back`, `brace → body:brace`.

### 1.2 BOM 쪽 — `test-utils/bom-parts.js`

- 같은 플래너에서 `buildPlannerPayload('PLANNER_DONE')` → `ui-step1.js _convertPlannerModules(payload, specs)`
  (planner-to-bom.test.js 와 같은 절단) → `MaterialExtractor.extract({items:[item]})`.
- 스펙은 `data-constants.js DEFAULT_SPECS` 의 싱크 부분 그대로(`LEDGER_SPECS`) — 목찬넬 손잡이, 좌·우 휠라 60.
  허용 목록이 스펙을 함께 저장하고 시험이 같은지 본다 (스펙이 달라지면 차이의 뜻이 달라진다).
- B1 `partId` 를 **아는 모듈 id 로 접두어를 벗겨** partKey 를 얻는다 (id 에 `-` 가 있어 쪼개 읽지 않는다 — §7-1).
  셀 모듈 `planner-X-i` 는 플래너 모듈 X 로, 멍장 `corner-blind-{pos}[-k]` 는 payload 순서의 k 번째 멍장으로 되돌린다.
  품목 단위 행(상몰딩·걸레받이·목찬넬·좌우 마감)은 `ep`.

### 1.3 대조 — `test-utils/ledger-diff.js`

단위는 **(플래너 모듈, family)** 다. 그 안에서 판 치수 다중집합을 ±1mm 로 짝짓는다.

| kind | 뜻 |
|---|---|
| `only3d` | 3D 에는 있는데 BOM 에 없는 종류 |
| `onlyBom` | BOM 에는 있는데 3D 에 없는 종류 |
| `dims` | 종류는 같은데 치수/수량 다중집합이 안 맞는다 (w/h 순서 없음, ±1mm) |
| `thickness` | 치수·수량은 맞는데 두께만 다르다 (지금은 0 건 — 두께가 다른 판은 치수도 달랐다) |

차이 id = `fixture|moduleId|family|kind`. 시험은 실제 차이 집합과 허용 목록이 **정확히** 같아야 통과한다:
목록 밖의 새 차이, 사라진 항목(낡은 허용), 내용(치수·수량)이 바뀐 항목, `cause`/`reason` 이 빈 항목 — 넷 다 실패하고 표를 찍는다.
갱신은 `UPDATE_LEDGER_ALLOWLIST=1 npx jest __tests__/scene-bom-ledger.test.js` (기존 항목의 cause·reason 은 id 로 이어받고
새 항목은 빈 이유로 들어와 다음 실행이 실패한다 — 사람이 적는다).

### 1.4 픽스처

| 이름 | 출처 | 내용 |
|---|---|---|
| straight | `test-utils/planner-golden.js` | 직선 하부 3공간(1200·1000·1400) · 상부 2 · 분배기 · 후드 |
| lShape | 〃 | ㄱ자 — 하부 회전 0/90, 상부, **키큰장 600 (3단 스택)**, 분배기 |
| oblique | 〃 | 135° 사선 — 하부 2공간(회전 0 · 135, 각각 2모듈) · 상부 1 (사선 공간도 자동계산이 채운다) |
| cornerL | `planner-corner-blind.test.js lShapeLayout(false)` 와 같은 배치 | ㄱ자 하부 3600 + 1970 → **멍장 1** + 수납 5 |

`test-utils/bom-golden/fixtures.js` 의 `sinkCorner` 는 상세설계 모듈 픽스처(플래너 배치가 아님)라 플래너에 부팅할 수 없다.
그 대신 cornerL 이 같은 멍장 경로(`corner-blind-lower`, 멍 665 · 도어 418)를 실제 자동계산으로 만든다.

## 2. 숫자

P2 수정 뒤 (P1 뒤 값은 괄호, C1a 첫 보고 #643 은 §2 아래 표):

| 픽스처 | 모듈 | 3D 행 (판) | BOM 행 (판) | 묶음 | 일치 | 차이 | only3d / onlyBom / dims |
|---|---|---|---|---|---|---|---|
| straight | 10 | 83 (80) | 74 | 99 | 27 (14) | 72 (85) | 25 / 16 / 31 |
| lShape | 11 | 91 (87) | 84 | 111 | 32 (20) | 79 (91) | 27 / 20 / 32 |
| oblique | 6 | 56 (55) | 51 | 69 | 18 (10) | 51 (59) | 18 / 13 / 20 |
| cornerL | 6 | 62 (60) | 52 | 74 | 22 (13) | 52 (61) | 22 / 12 / 18 |
| **합** | 33 | 292 (282) | 261 | **353** | **99** (57) | **254** (296) | 92 / 61 / 101 |

3D 행이 는 것은 처짐방지목이다 — 예전엔 양문 칸에만 한 장이라 단문 모듈(straight lower-1·lower-4·upper-3, lShape lower-4·tall 3단 …)엔
행이 없었는데 이제 모든 모듈에 있다. BOM 행·묶음은 그대로다 (3D 만 고쳤다). 일치가 42 는 것은 세 3D 오류 원인(§3 #4·#11·#13)이
사라진 만큼이다 — 42 묶음이 차이에서 일치로 옮겨 갔고 새 차이는 없다.

P1 뒤 (C1a 첫 보고 #643 의 값은 괄호):

| 픽스처 | 모듈 | 3D 행 (판) | BOM 행 (판) | 묶음 | 일치 | 차이 | only3d / onlyBom / dims |
|---|---|---|---|---|---|---|---|
| straight | 10 | 80 (99) | 74 (108) | 99 (105) | 14 (14) | 85 (91) | 25 / 19 / 41 |
| lShape | 11 | 87 (106) | 84 (121) | 111 (115) | 20 (16) | 91 (99) | 27 / 24 / 40 |
| oblique | 6 | 55 (68) | 51 (75) | 69 (68) | 10 (10) | 59 (58) | 18 / 14 / 27 |
| cornerL | 6 | 60 (70) | 52 (73) | 74 (74) | 13 (13) | 61 (61) | 22 / 14 / 25 |
| **합** | 33 | 282 (343) | 261 (377) | **353** (362) | **57** (53) | **296** (309) | 92 / 71 / 133 |

BOM 행이 는 것은 상판(픽스처마다 1)·키큰장 좌대 상자·목찬넬·상몰딩(lShape) 이다. 묶음이 는 것은 그 새 행이 3D 와 다른 묶음
(`ep|top`, `tall-0|pedestal:fb/side/kick`)에 들어가기 때문이고, 일치가 는 것은 키큰장 중간·상부 단 도어와 하부 단 목찬넬이 맞게 됐기 때문이다.

**이미 맞는 것** (일치 99 묶음): 모든 모듈의 측판 `body:side`(예 708×550×15 ×2), 표준 하부장 도어 `door`
(양문 396×678 ×2, 단문 396×678 ×1 …), 키큰장 세 단 도어(596×771 · 596×975 · 596×396 — P1-1 뒤), 키큰장 하부 단 목찬넬(600×52 · 600×40),
멍장 경첩목대 `body:batten-front/side`(708×60×15). P2 부터: **상부장 도어**(735×446 ×2 = 장H 720 + 15), **멍장 정면 셋**
(멍가림판 708×650×2.7 · 멍판 마감재 708×150×18 · 도어 678×414), **모든 모듈의 처짐방지목** `body:brace`(하부 608×70 ×2/×1 ·
상부 690×70 ×2/×1 · 키큰장 단 701/949/370×70 ×1 · 멍장 608×70 ×2).
즉 몸통 폭·높이와 도어 공식은 두 쪽이 같은 값을 낸다 — 대조 장치가 실제로 짝을 짓는다는 증거이기도 하다
(시험 "표준 하부장의 도어는 이미 맞는다").

## 3. 허용 목록 — 원인별

**254 건, 원인 10 묶음** (플래너 후속 멍장 도어 폭 뒤 — 2026-09-15 멍장 도어 결정 뒤는 255 건 11 묶음, P2 뒤는 254 건 10 묶음, P1 + 분배기 결정 뒤는 296 건 13 묶음, 첫 보고는 309 건 15 묶음). 판정: **BOM 버그**(발주가 틀린다) ·
**3D 오류**(도면이 규칙과 다르다) · **단순화**(3D 가 일부러 안 그리거나 어림한 것 — 부재 모델이 정리) · **확인 필요**(규칙 자체를 정해야 한다).
P1·P2 로 지워진 원인은 취소선 없이 "0 (P1)" / "0 (P2)" 로 남겨 무엇이 있었는지 보이게 했다. P2 뒤 **3D 오류 판정은 0 건**이다.

| # | cause | 건 | kind | 무엇이 다른가 | 판정 |
|---|---|---|---|---|---|
| 1 | 라인 단위 부재 | 87 | only3d 71 / onlyBom 16 | 걸레받이·상몰딩·목찬넬(전면·지면)·**상판**: BOM 은 품목 단위 `ep` 한 장(모듈 폭 합), 3D 는 런/모듈마다 한 장. 셈이 아니라 **묶음 단위**의 차이. P1 뒤 lShape 걸레받이·목찬넬은 4200(키큰장 단 제외), cornerL 상몰딩은 없다. 상판 14건(#9 였던 only3d 10 + `ep\|top` onlyBom 4)이 여기로 왔다 — **상판은 품목당 한 장으로 확정(사용자 결정 2026-09-15)**: 3D 가 런마다 한 장 그리는 것은 표시용 단순화이고, 이 14건은 의도된 묶음 차이로 남는다(§4 항목 3). 걸레받이·상몰딩·목찬넬 73건만 P3-10 대상 | 단순화 (상판 14건은 결정으로 종결) |
| 2 | 천판↔밴드 | 46 | only3d 23 / onlyBom 23 | 하부장·키큰장·멍장 몸통은 천판 없이 밴드 2장(70 × W−2T, sink.md). 3D 는 천판 통짜(W−2T × D−T) | 단순화 |
| 3 | 뒷판 모델 | 31 | dims | 3D 뒷판 15T 를 측판 사이에 끼움(W−2T × H). BOM 하부 2.7T 덧댐(W−2T × H−T), 상부 2.7T 사쿠리홈(W−20 × H−1) | 단순화 |
| 4 | 처짐방지목 | 0 (P2) | — | 3D 가 양문 가운데 장식용 한 장(60 × 도어H×0.9)만 그리던 것. P2-6 `addBraces` 가 BOM 규칙 그대로 — 70 × (H−2T), 목찬넬이면 −70 — 모듈 단위로 그린다. `BAND_BRACE_W` 60 → 70. 33 모듈 전부 일치. **장수·자리는 2026-09-19 결정으로 문턱 이상이면 1장·모듈 중앙, 미만이면 없음** (§0 참조) | 해결 |
| 5 | 선반 치수 | 30 | dims | 3D 선반 4mm 시각 여유(W−2T−4 × D−T−4). BOM 하부 W−2T × D−T, 상부 D−34(사쿠리), 멍장 −75 | 단순화 |
| 6 | 지판 깊이 | 23 | dims | 3D 지판은 15T 뒷판 앞에서 끝나 D−15. BOM 하부장 지판은 D 그대로(뒷판이 뒤에 덧대므로) | 단순화 (3 의 결과) |
| 7 | 사쿠리홈 깊이 | 16 | dims | 상부장 천판·지판: 3D D−15, BOM D−18 (3mm) | 단순화 (3 의 결과) |
| 8 | 가전 영역 모듈 | 0 (#648) | — | 분배기(`sink`) 배치 공간에 자동계산이 장을 세우던 것. 2026-09-15 "분배기는 씽크볼 위치 표시일 뿐" 결정으로 장을 세우지 않는다 (P4-11) | 해결 |
| 9 | 상판 미산출 | 0 (P1) | — | 상판(런 한 장, 12T)을 3D 는 그리고 BOM 은 없었다. P1-3 부터 BOM 이 품목 한 장(하부 폭 합 + 좌·우 마감 폭 × 품목 D × `topThickness`)을 낸다 — 남은 묶음 차이는 #1 로 | 해결 (묶음은 #1) |
| 10 | 밴드 미표현 | 8 | onlyBom | 상부장 밴드(보강목) 2장(W−2T × 70)을 3D 가 안 그린다 | 단순화 |
| 11 | 상부 도어 내림 잘림 | 0 (P2) | — | BOM 상부 도어 = 몸통 + 15 (오버랩). 3D 는 도어 내림 15 가 배치 공간 바닥 제한(`areaLimitsFor bottomLimit`)에 잘려 몸통 −4 였다. P2-4: `addFrontPanel` 이 제한을 규칙의 내림(`SINK_UPPER_DOOR_H_PLUS`)만큼 아래로 옮겨 그 너머만 자른다 — 도어 735 = 720 + 15 | 해결 |
| 12 | 스펙 마감재 | 8 | onlyBom | 좌·우 휠라 60 은 스펙 `finishLeft/RightType` 에서 나오고 도면에는 없다 (브리지가 경고만 한다) | **확인 필요** |
| 13 | 멍장 정면 치수 | 0 (P2 → 플래너 후속) | — | P2-5 까지: 3D 도어 칸 폭 = doorW + 목대 15 → 도어 429 (BOM 414), 멍가림판·멍판 마감재 높이에서 도어 갭 4 를 뺐다(704 vs 708), 멍판 폭 646 vs 650 → 멍 칸은 칸 폭 × 몸통 H 그대로, 도어는 doorW−4 로 맞춰 0 이 됐었다. **결정(2026-09-15, corner.md §3.5.2): 멍장 도어는 목대를 덮는다 — 재단 = doorW + 11.** BOM 이 먼저 옮겨(`agent/bom-blind-door-covers-batten`) cornerL 하부 멍장 도어가 다시 어긋난다: 3D 414(doorW−4) vs BOM 429(doorW+11). 정본은 §3.5.1 "마감재 다음 바로 도어" 로 확정됐고 3D 가 옛 그림이었다 — **플래너 후속(`agent/planner-blind-door-width`)에서 `createModuleMesh` 도어 칸을 `칸 폭 − 4 = doorW + 11` 로 되돌려 3D 429 = BOM 429, 이 1건이 사라졌다.** 2D 정면도도 같은 값(`planner-blind-door-width.test.js`) | 0 (플래너 후속) |
| 14 | 키큰장 단 규칙 | 0 (P1) | — | 브리지가 키큰장 단(중간장·상부장)을 `pos:'lower'` 로 보내 하부장 규칙을 탔다: 도어 H−30(3D 푸쉬 H−4), 선반 0 인 상부 단에 선반 1 강제, 걸레받이·목찬넬 폭에 3단 폭이 다 더해짐. P1-1 `addTallTierParts` (sink.md §5.1) 로 단별 도어·shelfCount·라인 폭 제외 — 세 단 도어가 모두 맞는다 | 해결 |
| 15 | 좌대 블록 모델 (← 좌대 미산출) | 4 | only3d 1 / onlyBom 3 | 키큰장 좌대: P1-3 부터 BOM 이 wardrobe.md 좌대 상자(전후·측·좌대 걸레받이)로 낸다. 3D 는 W×60×D 통짜 블록 하나라 서로 다른 묶음에 든다 | 단순화 ([확인 필요] 상자 구성) |
| 16 | 몰딩 블록 모델 | 1 | dims | 키큰장 상부단 상몰딩: BOM 은 MDF 18T 면판 60×W (P1-1), 3D 는 W×60×(D+18) 블록. 상부장 상몰딩도 같은 단순화지만 #1 의 묶음 차이에 가려 있다 | 단순화 |

같은 원인이 모듈마다 되풀이되므로 건수는 모듈 수에 비례한다. 원인 하나를 고치면 여러 픽스처의 항목이 한꺼번에 사라진다.

## 4. 수정 우선순위

건수가 아니라 **발주가 틀리는가**로 순서를 정했다. 각 항목은 고치면 허용 목록에서 지워져야 하고(시험이 낡은 항목을 막는다),
그 diff 가 곧 수정의 증거다.

### P1 — BOM 버그 (지금 발주가 틀린다) — **완료** (`agent/bom-p1-fixes`, 시험 `__tests__/bom-p1-fixes.test.js`)

1. ~~**키큰장 단 → 하부장 규칙**~~ (#14, #1 의 일부) **완료**. `extractSink` 하부 루프가 `bomTallTierOf(mod)` 로 단을 알아보고
   `addTallTierParts` (sink.md §5.1) 로 보낸다: `heightParts` 로 단을 가르고(맨 아래만 `pedestalH`, 맨 위만 `moldingH`), 도어는
   목찬넬 하부단 H−30 / 푸쉬 단 H−4, 선반은 `shelfCount` 그대로, 좌대 상자·목찬넬은 하부단에, 상몰딩은 상부단에, 라인 폭
   (걸레받이·목찬넬·상판)에서는 뺀다. 멍장 키큰장(`blindKind:'tall'`)도 같은 가지. 허용 목록 −5 (도어 2 · 선반 1 · 목찬넬 2),
   lShape 걸레받이·목찬넬 6000 → 4200.
   **남은 것**: `areaWidths` 가 비어 뜨는 "자동계산 전" 경고는 `ui-step1.js _cellsOfPlannerModule` 의 것이라 이 PR(BOM 도메인)에서
   안 고쳤다 — 스택은 셀을 안 나누므로 `section === 'tall'` 이면 경고를 건너뛰어야 한다 (Design UI 도메인, [확인 필요]).
2. ~~**상부장 없는 배치의 상몰딩**~~ (#1 cornerL) **완료**. `totalUpperW > 0` 일 때만 낸다. 허용 목록 −1 (cornerL `ep|molding` 4866).
3. ~~**상판·좌대 미산출**~~ (#9, #15) **완료 — 단, 묶음은 품목 단위**. 상판은 하부 라인 한 장(하부 폭 합 + 좌·우 마감 폭 × 품목 D,
   두께·자재 `specs.topThickness`, 코드 디테일 `top` 슬롯 > `specs.topColor` → `TOP-*`, slot `top`, `partId 0-ep-top-0`). 키큰장
   좌대는 wardrobe.md 좌대 상자로(1 번에 포함). 붙박이장 좌대는 이미 있었다 — 시험으로 잠갔다.
   #9 의 10건은 #1(라인 단위 부재)로 옮기고 `ep|top` 4건이 더해졌다. 3D 좌대 블록 ↔ BOM 상자(#15, 4건)와 키큰장 상몰딩 블록 ↔
   면판(#16, 1건)은 단순화로 남는다.
   **상판 묶음 — 결정으로 종결 (2026-09-15, `agent/bom-followups`)**: 상판은 **품목당 한 장**이다. "런(배치 공간)마다 한 장" 으로
   BOM 을 옮기자는 안은 철회됐고(브리지 `runId/runW` 계약도 없다), 3D 가 런마다 한 장 그리는 것은 **표시용 단순화**로 둔다.
   따라서 허용 목록의 상판 14건(only3d 10 · `ep|top` onlyBom 4)은 코드가 아니라 결정으로 닫힌 **의도된 차이**다 — `reason` 에
   그렇게 적었고, 항목은 지우지 않는다(지우려면 3D 를 품목 단위로 바꿔야 하는데 도면 표현은 런 단위가 맞다). P3-10 에서 상판은 뺀다.
   **[확인 필요]**: 오버행·물끊기·타공 규칙(계획 B2), 상판이 마감재 폭을 덮는지(3D 는 영역 전폭 — 그렇게 맞췄다), 키큰장 좌대 상자
   구성(wardrobe.md 를 빌림), 통짜 키큰장(상세설계 화면)의 선반 1 · 도어 한 장.

### P2 — 3D 오류 (도면이 규칙과 다르다; `mockup-structure.html` 3D 빌더) — **완료** (`agent/planner-p2-fixes`)

순수 3D 그리기 변경이다 — `buildPlannerPayload` 골든(I1)은 바이트 그대로, 피킹·보기 유지(I2) 시험도 그대로. 허용 목록 296 → 254 (−42).
2D 정면도(SVG `renderFrontView`)는 손대지 않았다 — 원장이 3D 만 보므로 정면도의 처짐방지목 표시(양문 가운데 한 장)는
아직 예전 그림이다 [확인 필요: 정면도도 같은 규칙으로 옮길지]. 멍장 도어 폭은 플래너 후속에서 3D 와 맞췄다(§4 항목 5).

4. ~~**상부 도어 내림 잘림**~~ (#11) **완료 −8**. `addFrontPanel` 의 제한을 `bottomLimit − SINK_UPPER_DOOR_H_PLUS` 로 옮겼다 —
   규칙의 내림 15 만큼은 배치 공간 바닥 아래로 나가고 그 너머만 자른다. 배치 공간·몸통 기하는 그대로. `planner-area-bounds.test.js` 가
   "딱 맞으면 내림 0" 이라는 잘못된 기하를 박아 두었던 것이라 규칙 근거와 함께 고쳤다. `doorSpanOf`(휠라 세로 범위)는 여전히 0 에서
   자른다 — 휠라가 도어 내림을 따라 내려올지는 [확인 필요].
5. ~~**멍장 도어 15 과다**~~ (#13) **완료 −3**. `createModuleMesh` 가 멍 칸은 `colW × topH`(갭 없음), 도어 칸은 `colW − 15 − 4` 를
   목대 반(7.5)만큼 멍 반대쪽으로 옮겨 그린다. 3D 에서 마감재와 도어 사이에 목대 15 + 갭 2 가 보인다 — corner.md §3.5.1 은
   "마감재 다음 바로 도어" 라 하고 BOM 재단은 doorW−4 다. 여기서는 발주 치수(BOM)를 따랐다 [확인 필요: 둘 중 정본].
   **→ 결정 (2026-09-15): §3.5.1 이 정본이다 — 멍장 도어는 목대를 덮는다.** 도어 재단 = 도어 자리(doorW + 목대 15) − 갭 4 =
   **doorW + 11** (corner.md §3.5.2). BOM 쪽(`agent/bom-blind-door-covers-batten`)이 먼저 옮겼다: `corner-engine.js blindDoorPartW` ·
   `deriveCorner().doorPartW`, `extractors.js blindDoorPartW` (하부·상부·키큰장 단 멍장 도어 행), 정면 원장 `멍 + doorW = 멍장 W` 를
   `assertCornerLedger` 가 지킨다. sinkCorner 골든 도어 407 → 422 · 441 → 456. 경첩목대·멍가림판(멍−15)·마감재 150 은 그대로다.
   **3D 후속 — 완료 (`agent/planner-blind-door-width`)**: `createModuleMesh` 도어 칸을 P2-5 이전의 `칸 폭 − 4 = doorW + 11`
   (갭 좌우 2, 목대 15 는 도어 뒤)로 되돌렸고 멍 칸(갭 없음·몸통 H)은 P2-5 그대로다. 허용 목록의
   `cornerL|corner-blind-area-lower-1--area-lower-0|door|dims` 1건이 사라졌다 (255 → 254). 2D 정면도(`renderFrontView`)의 도어 rect 는
   원래 `칸 폭 − 4 = doorW + 11` 이었고, 멍판 box 기준만 칸 시작(xAcc)으로 옮겨 3D 처럼 갭 없이 덮는다 —
   `planner-blind-door-width.test.js` 가 2D 도어 폭·자리와 3D mesh 폭을 잠근다.
6. ~~**처짐방지목**~~ (#4) **완료 −31**. `addFrontPanel` 양문 가지에서 떼어 모듈 단위 `addBraces` 로: 70 × (몸통 H − 2T) × 15T,
   상부 W≥700 / 하부·키큰장 단 W≥800 이면 2장 아니면 1장(단문 포함), 목찬넬이면 −70. 자리는 BOM 이 정하지 않아 도어 바로 뒤에
   폭을 (장수+1) 로 나눈 자리에 세웠다 [확인 필요: 실제 조립 위치]. 대상은 싱크 라인(lower·upper·tall)과 그 멍장 — 붙박이장·냉장고장
   규칙엔 처짐방지목이 없어 그리지 않는다(예전엔 양문이면 섹션 불문 한 장). `BAND_BRACE_W` 60 → 70 (OBJ 측정값이 3D 만의 값이었다).

### P3 — 단순화 (부재 모델 C1b 가 한 정의로 정리)

7. **몸통 뒷면 모델** (#3 뒷판, #6 지판 깊이, #7 사쿠리 — 합 70 건). 3D 가 뒷판을 15T 끼움으로 그려 지판·천판·선반 깊이가
   전부 D−15 로 밀린다. 부재 모델이 "뒷판 2.7T 덧댐(하부) / 사쿠리홈(상부)" 을 정의하면 세 원인이 함께 사라진다.
   **C1b 첫 이전 후보** — 코너/멍장 다음에 몸통을 옮길 때 이것부터.
8. **천판↔밴드** (#2, 46 건) · **밴드 미표현** (#10). 하부·키큰장 천판 자리는 밴드 2장, 상부장은 천판 + 보강목 2장.
9. **선반 4mm 여유** (#5). 3D 만의 시각 여유 — 부재 모델에서는 재단 치수를 갖고 그릴 때만 줄인다.
10. **라인 단위 부재** (#1 의 걸레받이·상몰딩·목찬넬 73 건 — 상판 14건은 P1 항목 3 의 결정으로 뺀다). 걸레받이·상몰딩·목찬넬을
    "런/라인 한 장" 으로 정의하면 3D 도 BOM 도 같은 묶음이 된다. 3D 의 런 단위(영역 첫 모듈에 한 장)가 BOM 의 품목 단위보다
    제작에 가깝다 — BOM 을 런 단위로 옮기는 쪽. 단, 상판은 그 반대(품목당 한 장)로 결정됐으므로 이 항목은 상판을 따라가지 않는다.

### P4 — 규칙 확인 필요 [사용자 결정]

11. **분배기 영역의 장** (#8). 자동계산이 `sink` 배치 공간에 장을 세운다. 개수대 하부장은 실제로 하부 라인 안의 셀(브리지가
    `개수대` 로 라벨)이므로 이 장은 중복이거나, 아니면 분배기 상판 밑 공간을 뜻하는 것이다. 어느 쪽이든 3D 와 BOM 중 하나는 틀리다.
12. **마감재의 출처** (#12). 좌·우 휠라는 스펙에서 나온다. 계획 D 축이 "도면에 그린 마감재(finishings)" 를 정본으로 하면
    스펙 값은 폴백으로 내려야 한다.

### 런 부재 멤버십 — **완료** (2026-09-16, `agent/planner-run-part-membership`, 시험 `__tests__/planner-run-part-geometry.test.js`)

원장 건수는 그대로지만(위 §0 참조) 이 문서가 P3-10 "라인 단위 부재" 에서 전제하는 **"런에 한 장"** 이 옛 설계에서
실제로는 지켜지지 않고 있었다 — 그것을 닫았다.

`addToeKick`·`addTopPanel` 은 영역을 `areaOfModule(m)` 로 찾아 놓고 그 영역의 모듈은 `x.areaId === area.id` 로 다시
모았다. `areaId` 가 없는 옛 설계에서는 **영역은 찾아지는데 멤버가 0명**이라:

- 걸레받이 — `Math.min(...[])`·`Math.max(...[])` 가 ±Infinity 라 폭 −Infinity · 자리 NaN 인 상자가 three.js 로 들어가
  페이지를 열 때마다 `computeBoundingSphere(): Computed radius is NaN` 이 떴다 (하부 모듈마다 BoxGeometry 1 + EdgesGeometry 1).
- 상판 — "런의 첫 모듈" 이 `undefined` 라 가드가 못 걸리고 **모듈마다** 영역 전폭짜리 상판을 그려 겹쳤다.
  폭이 `area.W` 라 NaN 이 안 돼 콘솔에는 안 보였다.

`modulesOfArea(area)` 하나를 두고 두 빌더가 **파일의 정본 규칙 `areaIdOfModule`**(areaId → 같은 단 겹침 →
단 구분 없는 `areaOfModule`)로 멤버를 모으게 했다. 처음 고칠 때는 `areaOfModule` 을 바로 썼는데, 그 함수는
**단을 가르지 않아** 0~1800 상부장이 0~1200 하부 영역의 멤버가 되고 걸레받이가 1800 으로 넓어져 옆 영역을
600 침범했다 (골든 straight, 2026-09-16 확인). 폭 검사는 구현의 `modulesOfArea` 로 기대값을 만들어 이 증상을
못 잡았으므로, 픽스처에서 읽은 **정답값(1200 · 1000 · 1400)** 을 박은 시험을 따로 두었다.
그리기만 바뀌므로 `buildPlannerPayload` 골든은 바이트 그대로다.

## 5. C1b 로 넘기는 것

- 부재 모델 `part-model.js` 는 modules + structures + detail 에서 **이 문서의 family 이름**으로 부재를 내야 한다
  (`body:side` `body:top` `body:bottom` `back` `shelf` `body:band` `body:brace` `door` `drawer` `blind` `blindfin`
  `body:batten-*` `channel:front/back` `top` `kick` `pedestal` `molding` …). 이름 표는 `ledger-diff.js KNOWN_FAMILIES`
  와 `bom-protocol.md §7-1` 이 정본이다.
- 이전 순서 제안: 코너/멍장(P2-5) → 몸통 뒷면(P3-7, 70 건) → 천판/밴드(P3-8, 54 건) → 라인 단위 부재(P3-10, 75 건)
  → 전면(P2-4·6). 각 이전 PR 은 허용 목록에서 항목을 지우는 diff 를 동반해야 한다.
- P1 은 부재 모델을 기다리지 않는다 — `extractors.js`/`ui-step1.js` 에서 바로 고친다 (BOM 도메인, 별도 PR).

## 6. 한계 (이 시험이 아직 안 보는 것)

- 붙박이장·냉장고장: 플래너가 그 구조를 만들지 않아(`_isNativeOnly`) 브리지가 막는다. 골든 BOM 시험이 따로 있다.
- 서랍: 자동계산이 `horizontalLayout:'doorOnly'` 로 만들어 픽스처에 서랍 셀이 없다 → `drawer`·`drawerbox:*` 는 대조 안 됨.
- 도면에 놓은 마감재(EP·몰딩·휠라 모듈)와 두 칸 이상 셀 모듈, 알루미늄 찬넬, 도어 내림이 실제로 그려지는 배치.
- 철물(다리발·경첩·레일)은 자재 원장 밖이다 (`HardwareExtractor`).
- `thickness` 종류는 정의돼 있지만 지금은 0 건 — 두께가 다른 판은 치수도 달라 `dims` 로 잡힌다.
- **옛 설계(`areaId` 없는 모듈)**: `bootPlanner3D` 는 `modules:false` + 전체 자동계산으로 부팅하므로 픽스처의 모듈에는
  언제나 `areaId` 가 붙는다. `dadam_struct_modules_v1` 에 그대로 실린 옛 모듈(= `modulesFromFixture`, 골든 payload 시험이
  쓰는 형태)은 이 원장이 보지 않는다 — 런 부재 폭 NaN 버그가 원장에 안 잡혔던 이유다.
  그 경로는 `__tests__/planner-run-part-geometry.test.js` 가 잠근다.
